---
const { id, class: className } = Astro.props;
---

<section
    id={id}
    data-section={id}
    class=`w-full mx-auto lg:w-[740px] flex flex-col gap-8 ${className}`
>
    <slot />
</section>
